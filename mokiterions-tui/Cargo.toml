[package]
name = "mokiterions-tui"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "mokiterions-tui"
path = "src/main.rs"

# `SPEC-MOK-002` fixes this dependency, its version and its feature set, and
# `ADR-MOK-002` decides them. Changing any of the three is a specification amendment,
# not an implementation choice. Every user-interface dependency lives here and nowhere
# else; `mokiterions-core` depends on nothing.
[dependencies]
mokiterions-core = { path = ".." }
ratatui = { version = "0.30.2", default-features = false, features = [
  "crossterm",
  "layout-cache",
  "underline-color",
] }
