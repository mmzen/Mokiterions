+++
id = "VREC-MOK-017"
type = "verification_record"
title = "Verification record for WO-MOK-016 at the commit that carries all eleven manual assessments"
status = "verified"
owners = ["assurance owner"]
created = "2026-08-21"
updated = "2026-08-21"
commit = "ecba9fee0776dde98ffda6d69d8d4c47f5cc1ad6"
git_object_format = "sha1"
worktree_state = "clean"
verified_at = "2026-08-21T12:46:42Z"
artifact_snapshot_sha256 = "6d09493e5c9a72d055e98ea9db07cf40e35263a788aa5c754549f9ec804b68a9"
evidence_paths = [
  "docs/engineering/simulation/evidence/WO-MOK-016/README.md",
  "docs/engineering/simulation/evidence/WO-MOK-016/amendment-approvals.md",
  "docs/engineering/simulation/evidence/WO-MOK-016/analysis/amendments.py",
  "docs/engineering/simulation/evidence/WO-MOK-016/analysis/census.py",
  "docs/engineering/simulation/evidence/WO-MOK-016/analysis/composition.py",
  "docs/engineering/simulation/evidence/WO-MOK-016/analysis/delegation.py",
  "docs/engineering/simulation/evidence/WO-MOK-016/analysis/entropy.py",
  "docs/engineering/simulation/evidence/WO-MOK-016/analysis/horizon.py",
  "docs/engineering/simulation/evidence/WO-MOK-016/analysis/identifier.py",
  "docs/engineering/simulation/evidence/WO-MOK-016/analysis/interface.py",
  "docs/engineering/simulation/evidence/WO-MOK-016/analysis/peak.py",
  "docs/engineering/simulation/evidence/WO-MOK-016/analysis/regions.py",
  "docs/engineering/simulation/evidence/WO-MOK-016/analysis/resolutions.py",
  "docs/engineering/simulation/evidence/WO-MOK-016/analysis/runs.py",
  "docs/engineering/simulation/evidence/WO-MOK-016/analysis/test-census.py",
  "docs/engineering/simulation/evidence/WO-MOK-016/analysis/updated-tests.py",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/COMMIT.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/capture-state.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/census.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/correction-fight-verb.md",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/cross-check.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/full/seed42-baseline-d0.75-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed0-baseline-d0.15-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed0-baseline-d0.15-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed0-baseline-d0.75-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed0-baseline-d0.75-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed0-baseline-d1.50-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed0-baseline-d1.50-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed0-individual-d0.15-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed0-individual-d0.15-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed0-individual-d0.75-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed0-individual-d0.75-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed0-individual-d1.50-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed0-individual-d1.50-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed0-reference-d0.15-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed0-reference-d0.15-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed0-reference-d0.75-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed0-reference-d0.75-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed0-reference-d1.50-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed0-reference-d1.50-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed1-baseline-d0.15-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed1-baseline-d0.15-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed1-baseline-d0.75-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed1-baseline-d0.75-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed1-baseline-d1.50-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed1-baseline-d1.50-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed1-individual-d0.15-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed1-individual-d0.15-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed1-individual-d0.75-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed1-individual-d0.75-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed1-individual-d1.50-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed1-individual-d1.50-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed1-reference-d0.15-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed1-reference-d0.15-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed1-reference-d0.75-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed1-reference-d0.75-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed1-reference-d1.50-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed1-reference-d1.50-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed123-baseline-d0.15-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed123-baseline-d0.15-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed123-baseline-d0.75-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed123-baseline-d0.75-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed123-baseline-d1.50-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed123-baseline-d1.50-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed123-individual-d0.15-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed123-individual-d0.15-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed123-individual-d0.75-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed123-individual-d0.75-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed123-individual-d1.50-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed123-individual-d1.50-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed123-reference-d0.15-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed123-reference-d0.15-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed123-reference-d0.75-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed123-reference-d0.75-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed123-reference-d1.50-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed123-reference-d1.50-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed42-baseline-d0.15-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed42-baseline-d0.15-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed42-baseline-d0.75-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed42-baseline-d0.75-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed42-baseline-d1.50-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed42-baseline-d1.50-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed42-individual-d0.15-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed42-individual-d0.15-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed42-individual-d0.75-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed42-individual-d0.75-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed42-individual-d1.50-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed42-individual-d1.50-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed42-reference-d0.15-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed42-reference-d0.15-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed42-reference-d0.75-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed42-reference-d0.75-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed42-reference-d1.50-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed42-reference-d1.50-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed777-baseline-d0.15-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed777-baseline-d0.15-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed777-baseline-d0.75-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed777-baseline-d0.75-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed777-baseline-d1.50-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed777-baseline-d1.50-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed777-individual-d0.15-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed777-individual-d0.15-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed777-individual-d0.75-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed777-individual-d0.75-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed777-individual-d1.50-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed777-individual-d1.50-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed777-reference-d0.15-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed777-reference-d0.15-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed777-reference-d0.75-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed777-reference-d0.75-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed777-reference-d1.50-traceoff.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/init/seed777-reference-d1.50-traceon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/manifest.py",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/pre-manifest.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/retain.py",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/summary.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/test-census.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/baseline/test-run.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/capture-horizon.sh",
  "docs/engineering/simulation/evidence/WO-MOK-016/capture-social.sh",
  "docs/engineering/simulation/evidence/WO-MOK-016/capture.sh",
  "docs/engineering/simulation/evidence/WO-MOK-016/completion-summary.md",
  "docs/engineering/simulation/evidence/WO-MOK-016/entropy.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/escalation.md",
  "docs/engineering/simulation/evidence/WO-MOK-016/identifier-sweep.json",
  "docs/engineering/simulation/evidence/WO-MOK-016/identifier.md",
  "docs/engineering/simulation/evidence/WO-MOK-016/manual-assessment.md",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/COMMIT.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/branches.md",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/byte-identity.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/capture-state.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/composition.md",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/composition.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/delegation.md",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/delegation.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/gates.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/horizon-memory.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/horizon.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/interface.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/long-horizon.md",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/merge-recheck.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/observer.md",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/post-manifest.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/reads.md",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/resolution-tables.md",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/resolutions.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/runs.md",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/runs.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/social-manifest.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/test-census-amended.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/test-census-master.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/test-census-merged.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/test-census-reconciliation.md",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/test-census.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/test-run-amended.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/test-run-master.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/test-run-merged.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/test-run.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/updated-tests.md",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/updated-tests.txt",
  "docs/engineering/simulation/evidence/WO-MOK-016/post/world-rules-unchanged.txt",
]

[relations]
verifies_work_order = ["WO-MOK-016"]
conforms_to = ["VER-MOK-016"]
+++

# Verified Verification Record

**This record is `verified`.** It names commit `ecba9fee0776dde98ffda6d69d8d4c47f5cc1ad6` as the candidate satisfying
`VER-MOK-016` for `WO-MOK-016`, and the 161 files under `docs/engineering/simulation/evidence/WO-MOK-016/` as the
evidence for it. The transition to `verified` is the accountable assurance owner's act on that evidence, taken by the
repository owner on 2026-08-21 in the same session that took the eight outstanding manual assessments, and recorded here
rather than inferred from anything else in the tree.

**What made it available.** `VER-MOK-016` declares eleven manual assessments and states its own standard: *"An
unrecorded assessment is an outstanding assessment, and this contract is not satisfied while any remains outstanding."*
On 2026-08-21 the owner recorded the eight that were outstanding — five as product owner, two as technical owner, one as
assurance owner — into the record blocks of `evidence/WO-MOK-016/manual-assessment.md`, and `WO-MOK-016`'s *The eight
manual assessments, recorded 2026-08-21* logs the eight acts. Commit `ecba9fe` is the commit that carries them. **Ten of
the eleven are now recorded and the eleventh is carried to `WO-MOK-017` with `REQ-MOK-060`, so none is outstanding**,
and the retention item that asks for them is satisfied by the records rather than by their preparation.

**The transition was taken on this record and was not taken on `VREC-MOK-016`.** That record is the candidate for
`4539601`, where eight of the eleven blocks are blank; a `verified` status there would have asserted satisfaction of a
contract its own candidate does not satisfy. `VREC-MOK-016` is therefore `superseded`, and this record is its successor.

## Why one work order carries two verification records

`VREC-MOK-016` was captured at `2026-08-21T11:55:45Z`, committed at `be4721d`, corrected at `f468fe7`, and pushed. The
eight assessments were taken afterwards. `WORKFLOW.md` provides exactly one transition for that situation: *"Only a
`ready` VREC may become `superseded` in the current model. It must record a UTC `superseded_at`, a non-empty
`supersession_authorized_by`, and one `superseded_by` relation to a distinct `verified` or `released` VREC whose
work-order set covers the old record. The old commit, object format, snapshot, evidence, work orders, and verification
contracts remain unchanged."* The owner authorized the supersession as accountable assurance owner and it is recorded in
that record's front matter at `superseded_at = "2026-08-21T12:57:39Z"`, with `superseded_by = ["VREC-MOK-017"]`. This
record's `verifies_work_order` is `["WO-MOK-016"]` and its `conforms_to` is `["VER-MOK-016"]`, so it covers the retired
record's sets exactly.

**Re-pointing the old record at this commit was the available shortcut and the model does not provide it.**
`VREC-MOK-015` states the rule this repository set in `VREC-MOK-011` and followed in `VREC-MOK-014`: *"A decision does
not re-measure provenance, and it does not re-point the record at the commit that carries the decision."* Editing three
fields of `VREC-MOK-016` would have been cheaper than this record by every measure. It is also the one thing the
supersession rule forbids, so every provenance field of `VREC-MOK-016` is exactly as its capture produced it, and every
provenance field here was measured afresh at `ecba9fe`. **No figure was carried across.**

**Two records over one work order is this repository's own precedent and not a novelty.** `VREC-MOK-014` and
`VREC-MOK-015` are both `verified` and both carry `verifies_work_order = ["WO-MOK-014"]`, the second bound to
`9599c0a9`, a merge commit, because *"a record for the merge is a new record"*. The identifier carries no claim of
correspondence either: `VREC-MOK-008` covers `WO-MOK-009`, and this record covers `WO-MOK-016`.

## The front matter is a capture's provenance, and this one was composed by hand

**`harnessctl capture-verification` cannot emit this record, and refuses from both positions.** Run in this checkout it
stops at `harnessctl: revision provenance requires a clean Git worktree`, because the worktree necessarily holds this
untracked record. Run in a clean checkout of the candidate it stops at `harnessctl: evidence file does not exist`,
because at `ecba9fe` this record does not exist to be validated against — and the tool's own alternative, committing the
record first and capturing from a clean tree afterwards, would bind the record to a commit whose only content beyond
this one is the record itself, which `WORKFLOW.md`'s rule that *"a record cannot contain the hash of its own commit"*
forbids from the other side. `VREC-MOK-015` and `VREC-MOK-016` documented the same pair of refusals and all three are
correct. So every field above was measured directly:

| Field | How it was measured |
|---|---|
| `commit` | `git rev-parse HEAD` at `ecba9fe`, which was committed at `2026-08-21 14:39:20 +0200` — before any figure below was taken. Its single parent is `f468fe7fcce7a8c57b547ed19bb80f33e11d2c88`, the last commit of the pushed branch |
| `git_object_format` | `git rev-parse --show-object-format` |
| `worktree_state` | `git status --porcelain` in a detached worktree checked out at `ecba9fe`: **0 entries**. In this checkout the same command returns this record and the supersession edit to `VREC-MOK-016`, both of which belong to the commit that carries them and are not tracked differences from `ecba9fe` |
| `verified_at` | the capture timestamp, as the template names it, and **not** the decision time. The capture read `2026-08-21T12:46:42Z`. The owner's eight assessment decisions and the transition decision were taken in the session that surrounds it, and neither is timed by this field |
| `evidence_paths` | `git ls-tree -r --name-only ecba9fe -- docs/engineering/simulation/evidence/WO-MOK-016`, sorted. **161 declared against 161 in the candidate's tree, with nothing left over on either side** — no declared path absent from the commit, and no file of the packet undeclared. The list is identical to `VREC-MOK-016`'s because `ecba9fe` adds no evidence file and removes none; it changes one file's contents, `manual-assessment.md`, which both records declare |
| `artifact_snapshot_sha256` | `python scripts/generate_harness_dashboard.py --root <worktree>` in that same detached worktree, whose leaf directory name equals this repository's because `build_snapshot` puts the checkout's directory name into the hashed document. **PASS, 131 artifacts, 473 relations, 0 errors, 13 warnings, snapshot `6d09493e5c9a72d055e98ea9db07cf40e35263a788aa5c754549f9ec804b68a9`**, equal to `sha256sum target/harness-dashboard/dashboard-data.json` |

**The 131 artifacts and 473 relations include `VREC-MOK-016` as `ready` and do not include this record.** That is what
the commit holds, and it is the correct graph for a figure of that commit. The supersession of `VREC-MOK-016` and the
creation of this record both live in the commit that carries them, which is later than `ecba9fe`; **neither can move
this digest**, because there is no copy of either in the tree it is a digest of.

## What this record claims

### The candidate commit

| | |
|---|---|
| Commit | `ecba9fee0776dde98ffda6d69d8d4c47f5cc1ad6` — *"gov(WO-MOK-016): record the eight outstanding manual assessments"* |
| Branch | `feature/phase-3-definition`, local at the moment of capture; the remote tip at that moment was its parent |
| Parent | `f468fe7fcce7a8c57b547ed19bb80f33e11d2c88` — a single parent; this is not a merge commit |
| Contains the merge | `259859dffe1f5f856e154263c48d8d1e04808903`, the merge of `origin/master` into this branch, **14 commits back** |
| Worktree | clean at the commit; `Cargo.lock` is blob `824003e1cbd4757f28d09849e2faca0a4546e99f`, byte-identical to `origin/master`'s |
| Contents relative to the merge | **34 files, +14,027 / −97, every one of them under `docs/`.** `git diff --numstat 259859d ecba9fe -- . ':(exclude)docs'` is empty |
| Contents relative to `4539601` | the three governance commits of this chain's tail: `VREC-MOK-016` created and corrected, and this commit's two files |

**The source tree of this candidate is byte-identical to the merge's**, which is what lets the packet's merge-tip
figures describe this tree exactly rather than approximately: `post/merge-recheck.txt` and `post/test-run-merged.txt`
were taken at `259859df`, fourteen documentation commits ago, and no line of Rust, no manifest, no lockfile, no
toolchain file, no workflow and no script has moved since. **The four source gates were nonetheless re-run here rather
than carried over**, including from `VREC-MOK-016`'s candidate three commits back, because a figure is a statement about
a tree and a reader is entitled to see the statement made about *this* tree.

**Why the candidate is this commit.** `WO-MOK-016`'s transition to `implemented` is part of what this record verifies —
commit-bound verification is classified `required` for that work order — and the eleven manual assessments are part of
what `VER-MOK-016` requires, so the candidate must be a commit that carries both. `4539601` carries the transition and
not the assessments; `ecba9fe` carries both, and existed before any measurement below.

### Where this commit stands relative to `master`

This is measured rather than assumed, because it moved while this work was in progress:

| | |
|---|---|
| Pull request #35 | **`MERGED` at `2026-08-21T12:25:30Z`** by the repository owner, base `master`, head `f468fe7`, merge commit `fa065cc27aa250bd93c586b0c61da789dab49e33` |
| `origin/master` tip | `fa065cc`, whose tree is `02a01bf7e6bc6cd735179180490f1a60c92d2194` — **byte-identical to `f468fe7`'s tree**, this being a merge that carried the branch whole |
| Merge base with `origin/master` | `f468fe7`, this branch's own commit: `master` now carries everything of this chain up to and including `VREC-MOK-016` |
| Is `ecba9fe` an ancestor of `origin/master`? | **No.** `git merge-base --is-ancestor ecba9fe origin/master` fails |
| `git diff --name-status fa065cc ecba9fe` | **two files, +136 / −51** — `evidence/WO-MOK-016/manual-assessment.md` and `work-orders/WO-MOK-016.md` — and **nothing in the other direction**: no file `master` holds is absent or altered here |
| Source difference from `master` | **none.** `git diff --numstat ecba9fe origin/master -- . ':(exclude)docs'` is empty |

**Three consequences are stated rather than left to be worked out.**

1. **The merge landed a `ready` record and eight blank assessment blocks on `master`.** That is the state
   `VREC-MOK-016` accurately describes, and `master`'s copy of it says so in its own words. This record and the
   supersession are the repair, and both are on this branch and not yet in `master`.
2. **`master` moving does not stale any figure in this packet**, and that is the reason the row above is measured:
   `master` moved *to* this chain's own tree. Every oracle here is a statement about a tree that `origin/master` now
   contains except for two documentation files, neither of which any oracle reads.
3. **This record's `verified` status reaches the candidate commit and nothing downstream of it.** The merge is not a
   verification act, `WORKFLOW.md` does not govern merging, and no release record binds this work.

### The gates, measured at this commit

| Command | Result |
|---|---|
| `cargo test --workspace` | exit 0 — **21 `test result:` lines, 264 passed, 0 failed, 0 ignored, 0 measured, 0 filtered out**, and 264 unique test names |
| `cargo fmt --all -- --check` | exit 0, no output |
| `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings` | exit 0, **0 warning and 0 error lines** — in the repository's own declared form, `--locked` included |
| `cargo tree -p Mokiterions -e normal --locked --offline` | exit 0, **one line** — the engine has no dependency to print |
| `python scripts/validate_engineering_artifacts.py --root .` | **PASS**, exit 0 — **131 artifacts, 0 errors, 0 warnings**, all four planes at `E0/W0` |
| `bash scripts/check_engineering_harness.sh` | **PASS** — 131 artifacts, **473 relations** |
| `python scripts/check_declared_dependencies.py --root .` | exit 0 — *"Every declared set matches its resolved graph. 8.4a-8.4d pass."* |
| `python scripts/generate_harness_dashboard.py --root .` | **PASS**, exit 0 — 131 / 473 / 0 errors / 13 warnings, snapshot `6d09493e…`, the figure this record declares |
| `python scripts/inspect_engineering_artifacts.py --root .` | exit 0 — 131 artifacts, 473 relations, **29 findings: 0 error, 13 warning, 16 info**. `decision_required` holds exactly one entry, **`VREC-MOK-016 [ready] assurance-review`**; `active_work` **empty**; `assurance_pending` **empty**; `definitions_pending` holds `WO-MOK-008` and `WO-MOK-017` |
| `python -m se_harness validate .` under the pinned 0.4.0 venv | **PASS**, exit 0 — the same four planes at `E0/W0`, from the version the repository pins and CI installs rather than the machine-wide install |
| `python -m se_harness preflight . --work-order WO-MOK-016 --phase review` | **PASS** — `WO-MOK-016 (implemented)`, commit-bound verification **required**, decided by the **engineering owner**. Its reading manifest names `REQ-MOK-051` through `REQ-MOK-059` and **not `REQ-MOK-060`**, which is the scope amendment of 2026-08-21 visible in the derived graph rather than only in prose |

**Two of those lines are worth reading twice.**

**Zero ignored and zero filtered out: a suite can be made to pass by not running.** The 264 names are the merge tip's
census, reconciled name by name in `post/test-census-reconciliation.md` §9 — 225 retained + 1 renamed + 39 added against
`master`'s 226 — so the total is accounted for on both sides rather than merely reported. The count is unchanged from
`VREC-MOK-016`'s, which is the expected result of three documentation commits and is stated because an unchanged figure
that was not re-measured is worth nothing.

**The lint and dependency gates were re-run in the repository's declared form after a first run in a narrower one.**
`preflight`'s own output declares `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings` and
records that `--locked` *"is not optional"*; the first run of this candidate's clippy gate omitted `--all-features` and
`--locked`, and `cargo tree` was first run without `-e normal --locked --offline`. Both were re-run in the declared
form, both are exit 0, and the narrower first runs are disclosed rather than quietly replaced.

**`decision_required` holding `VREC-MOK-016 [ready] assurance-review` is the state of the commit and not of the tree
today.** At `ecba9fe` that record is `ready` and this one does not exist, so the queue names the act the owner then took.
In the working tree that carries the supersession and this record, the same queue **empties**.

### The eight oracles, at this commit

`VER-MOK-016`'s *Independence* section declares eight and forbids any of them standing for another. Each is retained,
each names the reader that produced it, and **none was recomputed for this record** — the tree they read is byte-identical
to the one they were taken against.

| Oracle | Retained in | What it returned |
|---|---|---|
| 1 — a recorded pre-change capture, compared without projection | `baseline/` (`capture-state.txt` PASS 90/90, `cross-check.txt` PASS 90/90 and 212/212 against `VER-MOK-011`), `post/byte-identity.txt`, `post/capture-state.txt` (PASS 90/90 and 30/30) | **`RESULT: MIXED`.** The `baseline` clause passes on **30 of 30** cells, byte for byte, unprojected. The 60 `reference` and `individual` cells are **also** byte-identical, which is not what the oracle asks for — it asks for a characterized divergence, and there is none, *"which is `REQ-MOK-060` measured as unimplemented"*. See *The re-classification this record carries* |
| 2 — the shared entropy stream's own position | `entropy.txt`, `analysis/entropy.py` | **PASS, every check.** Fifteen recorded pairs reproduced; seed 0 at density `0.75` at **270 draws**, state `0xDE8261A4408EDE26`; the reconstruction checked against **90 retained initialization excerpts**; **33 resolution call sites over 15 seeds** with the state before equal to the state after; every draw site the shipped engine has enumerated, so branches 1–5's no-draw constraint is structural and not observational |
| 3 — constructed adjacency, for exact arithmetic | `post/resolution-tables.md`, `post/resolutions.txt`, `analysis/resolutions.py` | exit 0 — **24 of 24 retained rows and 1,742 of 1,742 released resolutions agree with the tables.** Three columns from three sources: the value computed from `SPEC-MOK-001` on paper, the engine's constructed-state value, and the released capture's. Eleven controls, six in the reader and five in the engine, each reverted |
| 4 — whole runs, for reachability | `post/runs.md`, `post/runs.txt`, `post/branches.md`, `analysis/runs.py` | **118,201 proposals, 118,157 applications.** All seven targeted verbs apply somewhere; at least one combat death on **every** declared seed (1, 2, 2, 3, 1); 102 survivors and 78 deaths across the fifteen traced cells summing to 180 with none left over; **44 rejections, all `avoid` as `out_of_bounds`, all nine distinct cells on a world boundary**; 115 encounters, 1.36 strikes each, 58.3% bloodless |
| 5 — identifier symmetry and the turn-position bound | `identifier.md`, `identifier-sweep.json` (SHA-256 `7d7033c4…`), `analysis/identifier.py`, `escalation.md` §11 | **Both parts pass.** The exchange: one constructed encounter resolved with the two roles exchanged, outcome depending on roles and not identifiers. The bound: highest ÷ lowest survival across the six turn positions over the declared 200 seeds is **`1.0819`** against **`< 1.25`**, `0.168` of margin; `1.063` over all 1,000 seeds. The sweep's three series were produced twice by two independently written readers agreeing on all 1,000 seeds |
| 6 — an enumeration of reads, for `REQ-MOK-059` | `post/reads.md` | **48 candidate expressions classified, none omitted** — 46 found mechanically in the production half of `simulation.rs`, two added by hand because they search with `.find()`. Seven readers of a set survive, each outside the trigger with the reason enumerated rather than asserted. **Its completeness is manual assessment 10, and that assessment is recorded** |
| 7 — the governance state of what this change amends | `amendment-approvals.md`, `analysis/amendments.py` | exit 0, **`RESULT: PASS` on ten verdicts.** 26 chain artifacts at their expected status; **21 amendment provisions each found twice in disjoint text** — in the row, and in the artifact with every row deleted; **33 baseline rows classified 22 byte-identical / 8 moved only in the approval cell / 3 carrying nothing but the renumber / 0 with a moved operative cell / 0 absent**; the four superseded sentences retained and retained only as quotations; the `VREC-MOK-005` gate read at 11 of 11 provisions ratified and 6 of 7 assessments authored |
| 8 — delegation equality, observation by observation | `post/delegation.md`, `post/delegation.txt`, `analysis/delegation.py` | **PASS**, exit 0, 30 checked properties over 120 cells. Both the narrow and the widened form asserted. Oracle 8's stated consequence — a `social` run takes fewer draws than an `individual` run at the same seed — holds at **15 of 15 strictly lower, 0 undecided, 0 contradicted**, including the five sparse cells where both totals are open intervals and the intervals are still disjoint |

Oracle 7 is the one that could have failed regardless of the code, and it is the only reader in the packet whose subject
is the governance tree rather than the engine or its output. **It was taken before this record and before the supersession
existed**, so its 26-artifact status check does not cover either; what covers them is this record's own front matter and
the harness runs above, in which the graph validates at 0 errors with `VREC-MOK-016` at `ready` and, in the working tree,
at `superseded` with this record present.

### The coverage matrix, row for row

`VER-MOK-016`'s *Requirement-to-evidence matrix* declares **93 rows** over ten requirements — 71 `automated-test`, 20
`static-analysis`, 2 `review`.

| Requirement | Rows | State at this candidate |
|---|---:|---|
| `REQ-MOK-051` | 6 | satisfied |
| `REQ-MOK-052` | 12 | satisfied |
| `REQ-MOK-053` | 12 | satisfied |
| `REQ-MOK-054` | 9 | satisfied |
| `REQ-MOK-055` | 7 | satisfied |
| `REQ-MOK-056` | 9 | satisfied |
| `REQ-MOK-057` | 22 | satisfied, against the amended six-branch ordering with the engagement gate at `95`; **the technical owner's assessment 8 ratified the ordering and the four thresholds at this candidate** |
| `REQ-MOK-058` | 6 | satisfied — five of five seeds on **both** bounds simultaneously, survivors 9, 10, 9, 9, 11 against a floor of five and combat deaths 1, 2, 2, 3, 1 against a floor of one, produced by two independent paths |
| `REQ-MOK-059` | 5 | **satisfied.** The enumeration of oracle 6 carries the rows, and the completeness they rest on is manual assessment 10, **recorded 2026-08-21 by the assurance owner** at 48 reads classified and seven aggregate readers justified |
| `REQ-MOK-060` | 5 | **carried to `WO-MOK-017`. Not claimed here.** |
| | **93** | **88 claimed, 5 carried** |

**`REQ-MOK-059`'s five rows are the one line this record claims that `VREC-MOK-016` could not.** There they were
satisfied by an enumeration whose completeness was an outstanding assurance-owner act; here the act is recorded, and the
rows stand on the enumeration and the record of it together.

**The five carried rows are named rather than counted.** They are `REQ-MOK-060`'s three `automated-test` rows — *the
ceiling*, *measured from rule 18's summary*, and *the pre-change state is reproduced as the contrast* — and its two
`static-analysis` rows, *the correction is where it is permitted to be* and *no composition read by any source*. The
product owner deferred `REQ-MOK-060` on 2026-08-20 and the owner amended this work order's scope on 2026-08-21 to carry
it, with `WO-MOK-017` named as its destination. **`VER-MOK-016` still lists `REQ-MOK-060` in `verifies`, and this record
does not change that**: amending a verification contract is the assurance owner's act on that contract, and the
2026-08-21 decision was a scope amendment to a work order. So this record claims 88 of 93 and says which five it does
not claim.

**The measurement those five rows will be decided on is nonetheless taken and retained.** `post/composition.md` (exit 0,
35 checked properties over 120 cells) carries the full 60-row per-territory per-class table read from rule 18's own final
summary line, with no new event and no new instrumentation: **27 of 30 evaluations breach the ceiling of one half**, the
widest class is `high` in every one of the 27, the widest share runs 37.3% to 81.6%, and **17 of the 20 pre-change
evaluations breach it too**, read directly from `baseline/summary.txt` at `39662d13` rather than inferred. All 20
verdicts are identical at both commits, because on this measurement the candidate *is* the pre-change state.
`WO-MOK-017` therefore inherits the curve and not just the question. **That is a retained measurement and not a coverage
claim.**

### The change surface this record binds

`git diff --numstat 39662d13 13906153`, everything outside `docs/`: **fifteen files, 3,835 insertions, 213 deletions**,
of which `mokiterions-core/src/simulation.rs` is 2,842/157 and carries the whole behavior change with its internal-tier
tests. The public surface moves from **172 entries to 186** — `pub` declarations 49 → 49, public fields 43 → 43, trait
implementations 13 → 13, private fields 18 → 18, and **enum variants 48 → 62** — with **14 added, 2 changed, 0 removed**,
matching the approved `SPEC-MOK-002` amendment's four growth rows at 15 declared against 15 measured
(`post/interface.txt`, exit 0).

Two deviations from the work order's declared surface are measured rather than argued, in opposite directions:
`mokiterions-tui/src/state.rs` is named and carries **zero** changed lines, because `ingest` matches on the five details
the observer's state needs and ends `_ => {}`; and `mokiterions-tui/src/options.rs` changed at 1/1 and is **not** named,
because the observer parses its own arguments. Both are in `completion-summary.md` §1.2.

**No source line moved between `VREC-MOK-016`'s candidate and this one.** The surface figures are the same figures, and
they are the same because the tree is the same, not because they were copied.

### The eleven manual assessments, all recorded

`VER-MOK-016` declares eleven and states that an unrecorded assessment is an outstanding assessment. **Ten are recorded
and one is carried out of scope; none is outstanding.** The record blocks are in
`evidence/WO-MOK-016/manual-assessment.md`, each naming its role and date; `WO-MOK-016` logs the eight acts of
2026-08-21.

| # | Subject | Owed by | Recorded decision |
|---|---|---|---|
| 1 | The damage function and the striker's energy cost | product owner | **2026-08-21 — `10 + (energy + health) / 10` and the flat strike cost of `5` ratified as decided**, on 1.36 strikes per encounter, 58.3% of contacts bloodless, 21 of 156 strikes fatal, damage spanning 17–30 in the runs with `12`–`16` paper-only. The weak brake is ratified as weak; the asymmetric `debug_assert!` at `simulation.rs:2624` is carried and not closed |
| 2 | The forfeit's magnitude and its non-conservation | product owner | **2026-08-21 — `satiety / 2` ratified, and the destruction of the unheld part recorded as intended in both cases the arithmetic admits.** Neither case is reached in the declared matrix: no recipient stood at `satiety` `100` and no surrender occurred below `2`. The ordinary discard is measured at **90 of 95** surrenders in the fifteen traced cells (`post/branches.md` §5) and at **180 of 190** surrender events across all thirty cells of the released capture (`post/resolution-tables.md` §10) — two scopes of one property, and this record quotes both rather than either alone |
| 3 | The threat's magnitude | product owner | **2026-08-21 — the constant `30` ratified and its measured behavioural inertness accepted.** 620 of 620 threats applied; all 1,240 `threat_resolved` lines report `increase:0`, the `fear` saturation at `100` being reached. The inertness is a saturation property and not a magnitude property, so amending the constant would not have moved it; it is carried as a finding |
| 4 | The survivor floor of five and the lethality bound | product owner | **2026-08-20 — ratified** by `REQ-MOK-058`'s amendment row on the measured curve, against measured alternatives of three and two |
| 5 | The composition ceiling of one half, and the per-class-floor question | product owner | **carried to `WO-MOK-017`** with `REQ-MOK-060`. Its wording also has a defect this record repeats rather than repairs: *"measured corrected composition"* has no subject at this candidate, there being no corrected composition |
| 6 | The additivity cost | product owner | **recorded in both halves.** The `REQ-MOK-034` narrowing half was approved 2026-08-20. The second half was recorded **2026-08-21 as not assessable at this candidate and carried to `WO-MOK-017`**: it asks for a characterized divergence and 60 of 60 cells are identical, so its premise is false here |
| 7 | The fourth source's name landing identically in all three places | product owner | **2026-08-21 — confirmed.** `social` lands identically in `--help`, in the invalid-value diagnostic and in every captured stream; the absence clause is discharged in closed-set form at **210 of 210** policy tokens, so it is a statement about a closed set and not a failed search. The `cli.rs:102` diagnostic remains asserted by no test, and that is carried |
| 8 | The defender's decision rule and whether its branch distribution is degenerate | technical owner | **2026-08-21 — the six-branch ordering and the four thresholds ratified as decided, and no `INT-MOK-010` degeneracy finding is owed.** Not degenerate on any of the clause's three shapes: over 118,201 decisions the branches fire 135 / 7,579 / 58,441 / 763 / 13,308 / 37,975, and branch 1's 135 answers split 13 `fight` / 27 `retreat` / 95 `surrender`. **No branch never fires.** On the question `REQ-MOK-057` left open: **branch 1 may not decline to answer**, closed in favour of the implemented rule. Rule 26's accepted cost is recorded as **not observed** — 0 of 135 answers naming a non-living target, 0 rejections on `target_dead`, `target_unknown` or `out_of_contact` — which is not a claim it cannot happen, and it stays a residual |
| 9 | Cross-agent mutation against `ARCH-MOK-001` | technical owner | **2026-08-21 — confirmed satisfied unchanged**, in review against the implemented code. No architectural boundary moves, so the decision does not revert to an `ADR` and none is created |
| 10 | The completeness of oracle 6's read enumeration | assurance owner | **2026-08-21 — confirmed complete**: all 48 candidate reads classified, all seven surviving aggregate readers justified individually. `REQ-MOK-059`'s five rows rest on this |
| 11 | The monotonicity band's adequacy | assurance owner | **2026-08-20 — recorded**, *"and it was not adequate"*. The band was replaced by the two-part oracle 5, and this record reports the replacement's figures rather than the withdrawn band's |

**No decision among the eight was an amendment, so none changed a requirement.** Each either ratified a value already
recorded or confirmed a state already measured. `REQ-MOK-053`, `REQ-MOK-054`, `REQ-MOK-055`, `REQ-MOK-057` and
`ARCH-MOK-001` are unamended, and no `ADR` was created. **Three findings the ratifications carry rather than close** are
already enumerated in `completion-summary.md` §17.4 and restated in this record's residual section.

**Recording an assessment is not the same act as verifying this record**, and the eight are not the transition. The
eight were taken by the product, technical and assurance owners in their several roles; the transition of this record to
`verified` is the assurance owner's separate act, and `WORKFLOW.md` says so: a candidate *"moves separately from `ready`
to `verified` through an accountable human decision."* Both sets of acts are the same person here, because the
repository owner holds all three roles, and **that is a reason to name each act separately rather than to treat one as
implying another.**

**What the signatures leave stale in the retained evidence, stated rather than edited.** `manual-assessment.md` is the
one retained record whose format expects to be written into after capture, so its blocks and its own count of them moved
together. Two other files in the packet describe the assessments as they stood at `4539601` and are **not** edited,
because `SPEC-MOK-004` rule 11 requires evidence to be re-run rather than edited: `evidence/WO-MOK-016/README.md` at its
*What is owed* row, its two `manual-assessment.md` entries and its closing paragraph; and `completion-summary.md` at item
16 and §17.5. **Read at this commit those passages are stale in one direction only** — they state as owed a set of acts
that have since been taken, and they point at the file that holds them.

## The snapshot figure, and what it is a figure of

`artifact_snapshot_sha256` is `6d09493e5c9a72d055e98ea9db07cf40e35263a788aa5c754549f9ec804b68a9`: the digest of the
governed graph of commit `ecba9fe` exactly as that commit stands — **131 artifacts, 473 relations, 0 errors, 13
warnings, PASS** — measured in a detached worktree checked out there, with `git status --porcelain` returning nothing and
the worktree's leaf directory name equal to this repository's.

**One figure serves, because this record does not exist at the commit it binds.** `VREC-MOK-014` had to publish two
digests for `65ac88b` — one with the record held out of the tree and one with it present — because it already existed
there. This record arrives in a later commit, so the as-committed figure and the record-absent figure are the same
number. **No later edit to this file can move it**, including a future transition, because `build_snapshot` hashes
normalized front matter and there is no front matter of this record in that tree to normalize.

**No digest is given for the commit that carries this record, and none can be.** `build_snapshot` writes
`git rev-parse HEAD` into the hashed document, so that digest does not exist until the commit does, and a figure measured
beforehand would be a working-tree figure presented as a commit figure.

**Evidence enters the digest by path and not by bytes.** `build_snapshot` hashes normalized front matter, relations and
findings together with the declared evidence paths; it does not read the evidence files' contents. The 161 paths are
identical at `4539601` and at `ecba9fe`, and `manual-assessment.md`'s 6,761 new bytes are therefore invisible to both
digests. **The two digests differ — `71468119…` there and `6d09493e…` here — for the reasons that are in the hashed
document: the `HEAD` hash, and `VREC-MOK-016`'s own front matter, absent at `4539601` and present at `ecba9fe`.** A
reader who expects the assessment records to be what moved the number has the mechanism wrong, and this record says so
rather than leaving the inference available.

**The digest is reproducible from a full-depth checkout and from no other kind, and that is measured at this candidate
rather than carried from the last record.** `.github/workflows/engineering-harness.yml` uses `actions/checkout@v4` with
no `fetch-depth`, so CI reads this tree at depth 1. A depth-1 clone of this branch at `ecba9fe` reports **131 artifacts,
473 relations, 0 errors, 29 warnings** and snapshot `16d6c442dd…` — **a different digest from the one this record
declares** — and the inspector run in that clone names the whole difference: **`W-REV-003` at 16 observations**, one for
each of the fifteen verification records and `RLS-MOK-001`, every one of them declaring a candidate commit a shallow
clone cannot reach. `W-REV-003`'s own remedy is *"make the declared candidate commit available for assessment without
changing its recorded identity"*, which is a statement about the checkout and not about any record. **A reader comparing
this record's digest against a shallow clone will not reproduce it, and the discrepancy will be the clone's.** No
workflow was edited to change this, because the managed harness is not this work order's to amend; it is reported as a
finding.

## The re-classification this record carries

`post/byte-identity.txt` records `RESULT: MIXED`, and `WO-MOK-016`'s scope amendment of 2026-08-21 places the
re-classification of that result **in the work order and in the verification record** rather than in the evidence. So it
is stated here as `VREC-MOK-016` stated it, this record being the one now in force:

**The measurement was correct when taken and is correct now.** The `baseline` clause passes on 30 of 30 cells; the
`reference` and `individual` clause is unsatisfied because the divergence oracle 1 asks to be characterized does not
exist. What the descope changes is the reading, not the figure: the missing divergence is no longer an unmet obligation
of this work order but **the expected result of a world this work order leaves alone**. The file is retained verbatim,
because evidence is re-run and not edited.

**Two sentences inside that retained file are stale**, and they are recorded rather than corrected:

1. §3 closes *"So this section is a recorded failure of an approved requirement … and `WO-MOK-016` cannot reach
   `implemented` while it stands."* **Falsified by the scope amendment**, which is the act taken for exactly that reason.
2. §4 calls `post/social-manifest.txt`'s thirty `social` cells recorded *"separately and provisionally"*. The provisional
   label was withdrawn when those thirty were retaken at `59d61b91` and digest-matched at 30 of 30.

Neither sentence carries a figure and no figure in the file moves.

**`REQ-MOK-034`'s narrowing amendment stands and is untouched.** It narrowed the frozen-outcome constraint to `baseline`
alone so that `REQ-MOK-060` would be free to move `reference`. `reference` has not moved, so the narrowing is a
permission granted ahead of its use — and an unused permission is not a defect.

**`REQ-MOK-014`'s and `REQ-MOK-034`'s floors are preserved rather than re-established.** All 60 `reference` and
`individual` cells are byte-identical to the pre-change capture; identical output is identical survivors; so the
conditional amendment those requirements anticipated never triggers, and `REQ-MOK-014` correctly carries no 2026-08-20
or 2026-08-21 amendment row.

**`SPEC-MOK-001`'s rule 21 amendment row of 2026-08-20 points a reader at `VREC-MOK-016` for the correction it retains,
and that pointer is not edited.** The record it names is `superseded` and retained, its text unchanged, and the reading
it carries is repeated here; a reader following the pointer arrives at a record whose own opening sends them on to this
one. The row is an approved specification's, and amending it is the specification's owner's act and not this record's.

## Scope of the transition taken

**What `verified` asserts here, exactly.** That commit `ecba9fe` satisfies `VER-MOK-016` for `WO-MOK-016` on the
evidence declared above: 88 of the contract's 93 coverage rows claimed and 5 named as carried, all eight oracles
returning what the table records, the eleven manual assessments recorded or carried with none outstanding, and the eleven
gate commands at the results listed. It is a decision on that evidence at that commit.

**What it leaves where it was.** `WO-MOK-016` stays `implemented` — verification does not move a work order's status.
`VER-MOK-016` is unamended, `REQ-MOK-060` still appears in its `verifies` list, and the five carried rows stay carried.
`VREC-MOK-016` stays `superseded` and retained. No specification, requirement, architecture or interface artifact is
amended by this record, and it creates no `ADR`.

**What it is not.** It is not a merge decision, not a tag, not a release, and it makes no commit release-eligible. The
merge of pull request #35 preceded this record by twenty-one minutes and is not part of it: `WORKFLOW.md` does not
govern merging, and `ecba9fe` is not an ancestor of `origin/master`. **A reader who takes this record as a statement
about `master` has read it wider than it was given** — what `master` carries today is this chain up to `f468fe7`, with
`VREC-MOK-016` at `ready` and eight assessment blocks blank.

## What this record does not claim

- **It claims no coverage for `REQ-MOK-060`.** Five matrix rows are carried to `WO-MOK-017` and named above. The
  retained composition curve is a measurement offered to that work order, not a verdict withheld from this one.
- **It takes no manual assessment.** The eight of 2026-08-21 were taken by their accountable roles before this record
  was composed, and this record reports them; assessment 4 and 11 are carried over as recorded 2026-08-20; 5 and the
  second half of 6 are carried to `WO-MOK-017`. **Reporting a decision is not taking it.**
- **It ratifies no amendment and approves none.** `amendment-approvals.md` §5 names the two provisions this packet owes
  and does not make: `SPEC-MOK-004` rule 11's own figures, and `SPEC-MOK-003`'s reconciliation row of 2026-08-20, which
  stands on an implementation agent's statement of fact with no owner act. Both are still open and both are left for
  their owners.
- **It corrects no committed record.** The two stale sentences in `post/byte-identity.txt`, `post/gates.txt` §3's
  reliance on a prohibition `ADR-MOK-006` withdrew, §5's four-manifest claim falsified at the merge, `README.md`'s and
  `completion-summary.md`'s statements that eight assessments are owed, and the stale three-word comment in
  `mokiterions-core/tests/cli.rs` are all recorded and none is edited, on `SPEC-MOK-004` rule 11's requirement that
  figures be re-derived rather than edited.
- **It does not verify `master`'s observer work.** The 682 non-comment lines `WO-MOK-013` added to
  `mokiterions-tui/tests/render.rs` arrive with the merge and are `master`'s; `master`'s own records cover them.
  `post/merge-recheck.txt` §7 measures that none of the 14 cases they added asserts the row this packet's second
  observer residual names.
- **It holds no rendered frames with cell positions.** `post/observer.md` §9's overlay figures are arithmetic on the
  retained layout constants and the file says so in its own words. Whether a given terminal clips a given row is
  unmeasured.
- **It does not close `VREC-MOK-005`.** Its outstanding amendments and assessments are a precondition of this work under
  oracle 7 and not an output of it, and the gate is measured at 11 of 11 provisions ratified with 6 of 7 assessments
  authored — progress on the row and not the row closed.
- **It does not re-measure `VREC-MOK-016`'s candidate.** `4539601`'s figures are that record's and stay there; nothing
  here restates them as this candidate's.

## Residual uncertainty this record carries forward

`VER-MOK-016`'s nine residual bullets all stand as written; the first carries its own *Amended 2026-08-20* note. Beyond
them, this record carries forward what the packet holds and what it does not — including three items the 2026-08-21
ratifications explicitly carried rather than closed:

- **Rule 23's threat constant is behaviourally inert at the measured `fear` levels** — all 1,240 `threat_resolved` lines
  at `increase:0` — and assessment 3 ratified it in that knowledge. The cause is saturation at `100`, not magnitude, so
  no value of the constant fixes it; what would is a change to the attribute's ceiling or to the rule, and neither is in
  this work order's scope.
- **The asymmetric `debug_assert!` at `simulation.rs:2624`** and the suffered-record bound left unasserted stand as
  `completion-summary.md` §17.4 records them; assessment 1 ratified the damage function without closing either.
- **The invalid-policy diagnostic string at `cli.rs:102` is asserted by no test**, and assessment 7 confirmed the name
  in that diagnostic by reading captured streams rather than by a test that would fail if it changed.
- **Rule 26's accepted cost is not observed rather than unreachable** — 0 of 135 branch-1 answers named a non-living
  target, and 0 rejections carry `target_dead`, `target_unknown` or `out_of_contact`. Assessment 8 recorded it as not
  observed, which leaves the cost unpriced by measurement.
- **`1.25` is not established as the right bound**, only where it came from and that it holds at `1.082` with `0.168` of
  margin. The rule 25 ablation that priced the alternative is **not reproducible from a retained script**, and
  `identifier.md` §8 states `INT-MOK-010`'s risk in the opposite direction from the interface's own wording.
- **At 10,000 ticks the five seeds leave 0, 5, 2, 2 and 0 survivors** against a floor of five, two by extinction before
  the horizon. `REQ-MOK-058` binds at 1,000 ticks and is met there, so this is outside its trigger and is retained *"as
  evidence and not as an obligation"* — but *"four survivors of margin at the worst seed"* is a property of the
  1,000-tick curve alone, and whether the world should still be populated at 10,000 is a question this packet raises and
  does not answer.
- **The composition ceiling is breached at 27 of 30, at a drift of 31.3 points against 17.2 of headroom** — roughly
  twice the headroom the choice of one half was argued from. Outside this work order's scope since 2026-08-21, and
  `WO-MOK-017`'s to decide with assessments 5 and 6b.
- **Rule 24's forfeit divisor is an unnamed literal `2`** at `simulation.rs:2741` where the other three magnitudes are
  named constants, and the comment above the two appended assertions in `mokiterions-core/tests/cli.rs` still says the
  help *"can neither hide the third source nor advertise a fourth"*, where both counts are now wrong. Both are recorded
  rather than fixed, because fixing either moves the tree every figure here is bound to.
- **The observer's two residuals in `post/observer.md` §6 did not narrow, and the merge measured that they did not.**
  Both are one assertion away from closed, and closing either moves the reconciled census.
- **No release-side instrument reports whole-run draw totals per source.** `shared_stream_draws` is a test helper;
  `post/delegation.md` §6 recovers the totals rather than reading them off an instrument.
- **`completion-summary.md` sits in this packet on `WO-MOK-016`'s authority and not on `VER-MOK-016`'s**, whose
  retention table lists no completion report; and its §15 quotes each amendment row's operative clause rather than the
  row whole, which is a narrower reading of the declared format than its wording. Both are disclosed in that file.
- **Fourteen stop-and-escalate conditions are declared and none of the fourteen fired.** Three items the section
  discloses in advance stand: `VREC-MOK-005`'s gate; `doctor` exiting `1` on a **local** harness version skew against the
  `0.4.0` the repository pins, where `python -m se_harness validate .` passes; and
  `mokiterions-tui/tests/export.rs`'s `a_written_file_holds_exactly_the_rendered_records` being intermittently flaky on
  this platform, as `WO-MOK-011` disclosed — reported as residual rather than adjusted, because a test change is itself a
  stop condition here.
- **CI's dashboard warning count is 29 where this record declares 13**, for the depth-1 reason measured above. The
  remedy is upstream in the harness distribution or an owner decision to deviate; the managed workflow was not edited.

Every command behind every figure in this record is offline, reads no credential, secret, token or environment value,
and none appears in the retained evidence.
