# VER-MOK-018 cases L21a, L21b, C2 and A6: the workflow scan, run at the candidate
#
# command:    python scripts/check_workflow_credentials.py --root .
# self-test:  python scripts/test_check_workflow_credentials.py  ->  Ran 38 tests OK
# candidate:  6e9ca13ba70ec46696113bb742f45d78d602d41e
# run on:     2026-08-29, natively on Windows 11
# exit:       0
#
# The instrument is unchanged from the one WO-MOK-025 verified; only the tree under it moved.
# It is the gate .github/workflows/provider-credentials.yml runs on every push, so this reading
# is the same reading CI takes and not a second one taken by hand.
#
# What each case takes from it:
#
#   L21a  no workflow carries a model-provider credential and none selects live mode.
#         Scans A, B, C and D below, over 4 files and 637 non-comment lines.
#   L21b  a workflow step exercises the model-backed source in replay mode against a committed
#         transcript. Scan E, and the instrument's own CHECKED paragraph names the step and the
#         transcript. The transcript was confirmed to be a file in this repository and was not
#         read by the scan, which is why the step is a replay and not a live run.
#   C2    the credential appears in no workflow. The same absence as L21a's first half, read as a
#         containment property rather than as a configuration one.
#   A6    the four workflows are the whole of .github/workflows, so the scan's denominator is the
#         surface and not a sample of it. The four are named in the output.
#
# What it does NOT establish is in its own last paragraph, unedited below: whether a provider
# credential is present in the repository's *Actions secrets* is not visible from inside the
# repository at all. That is C6, an owner attestation, and credential-attestation.md carries it.
#
# ---- the instrument's output, byte for byte ----

Definitions under .github, at C:\Users\mathi\Mokiterions-understand-20260829-1340:
  .github/workflows/dependency-declarations.yml
  .github/workflows/engineering-harness.yml
  .github/workflows/provider-credentials.yml
  .github/workflows/release.yml
  4 file(s), 637 non-comment line(s) scanned
PASS: no workflow references a model-provider credential and none selects live mode
  scan A  no secret outside GITHUB_TOKEN
  scan B  no provider name and no credential-shaped environment name
  scan C  no step that brokers a secret from outside the repository
  scan D  no live-mode option, and every selection replays a committed transcript
  scan E  the model-backed source is exercised, in replay mode:
          .github/workflows/provider-credentials.yml:150 replays mokiterions-core/tests/transcript-seed0-ticks20-hunting.jsonl
CHECKED: VER-MOK-018 case L21b, that a workflow step exercises the model-backed source in
  replay mode against a committed transcript. The step(s) above are that case; each named
  transcript was confirmed to be a file in this repository, and none of them was read here.
NOT CHECKABLE HERE: whether a provider credential is present in the repository's Actions
  secrets. REQ-MOK-073's *Constraints* records that as the condition this containment rests on,
  and VER-MOK-018 carries it as an owner attestation.
