# WO-MOK-012 amendment approvals — oracle 7

`VER-MOK-012`'s seventh oracle is the governance state of the artifacts this change amends:
*"`ADR-MOK-005` accepted, and the `ARCH-MOK-001`, `SPEC-MOK-001` and `SPEC-MOK-002`
amendments it requires approved, before this change is verified. Absence fails this contract
regardless of code state."* This file is generated by `analysis/amendments.py`, which reads
the artifacts and the git history; what it checks, and why each check is not a formality, is
in that script's header. The engine's public interface, which is the other half of the claim
that the interface grows by one parameter and no item, is `interface.txt`.

Everything below is measured against `de33d7440c323a98ac88db3fabaf87bea48ebf4e`, the commit
this work started from, and every row this work order adds is dated `2026-08-20`.

## 1. The chain's governance state

| Artifact | status at the base | status now | expected | updated | ok |
|---|---|---|---|---|---|
| `INT-MOK-009` | `draft` | `approved` | `approved` | 2026-08-20 | yes |
| `CAP-MOK-009` | `draft` | `approved` | `approved` | 2026-08-20 | yes |
| `REQ-MOK-042` | `draft` | `approved` | `approved` | 2026-08-20 | yes |
| `REQ-MOK-043` | `draft` | `approved` | `approved` | 2026-08-20 | yes |
| `REQ-MOK-044` | `draft` | `approved` | `approved` | 2026-08-20 | yes |
| `REQ-MOK-045` | `draft` | `approved` | `approved` | 2026-08-20 | yes |
| `REQ-MOK-046` | `draft` | `approved` | `approved` | 2026-08-20 | yes |
| `ADR-MOK-005` | `draft` | `approved` | `approved` | 2026-08-20 | yes |
| `SPEC-MOK-006` | `draft` | `approved` | `approved` | 2026-08-20 | yes |
| `VER-MOK-012` | `draft` | `approved` | `approved` | 2026-08-20 | yes |
| `WO-MOK-012` | `draft` | `in_progress` | `in_progress` | 2026-08-20 | yes |
| `ARCH-MOK-001` | `approved` | `approved` | `approved, amended` | 2026-08-20 | yes |
| `SPEC-MOK-001` | `approved` | `approved` | `approved, amended` | 2026-08-20 | yes |
| `SPEC-MOK-002` | `approved` | `approved` | `approved, amended` | 2026-08-20 | yes |
| `SPEC-MOK-004` | `approved` | `approved` | `approved, amended beyond the list` | 2026-08-20 | yes |

The second column is measured rather than asserted, and it is what separates the packet this
work order implements from the documents it amends: everything drafted for this chain was
`draft` at the base commit and was approved afterwards, in one act on 2026-08-20, while the
four documents that already existed were already `approved` and are amended in place. The work
order is `in_progress` and not `complete` because a work order is closed by a verification
record that binds a commit, and that record is written after the commit it names.

## 2. Each provision, in the record and in the body

Every provision `ADR-MOK-005`'s *Required amendments* section states is looked for twice — in
the amendment record's 2026-08-20 row, and in the document's body with every amendment row
removed — because a record that claimed an amendment the text does not carry would otherwise
satisfy both checks with one sentence.

### `ARCH-MOK-001`

- rows dated 2026-08-20: **1**, of which **1** name this work order
- approval recorded in the row: yes
- records that the implementation agent wrote the text and decided no substance: yes

| Provision | in the record | in the text |
|---|---|---|
| 1. *Components* item 1 gains the entry point’s sink duties | yes | 3/3 phrases |
| 2. *Components* item 2 gains the counters and record production | yes | 2/2 phrases |
| 3. The observation-surface paragraph gains the host-supplied sink | yes | 3/3 phrases |
| 4. *Dependency direction* distinguishes a destination from persistence | yes | 3/3 phrases |
| 5. *Data and control flow* gains the second, optional branch | yes | 3/3 phrases |
| 6. *Prohibited patterns* gains three | yes | 3/3 phrases |
| 7. *Determinism* extends to the record stream | yes | 2/2 phrases |
| 8. *Debuggability* extends to structured recording | yes | 2/2 phrases |
| 9. *Conformance checks* gains four | yes | 4/4 phrases |
| 10. *Related architecture and ADRs* gains `ADR-MOK-005` | yes | 2/2 phrases |
| 11. `addresses` gains two requirements; `conforms_to` gains `SPEC-MOK-006` | yes | 3/3 phrases |
| 12. `decision_assessment.rationale` records the decisions and stays `adr_required` | yes | 3/3 phrases |

### `SPEC-MOK-001`

- rows dated 2026-08-20: **1**, of which **1** name this work order
- approval recorded in the row: yes
- records that the implementation agent wrote the text and decided no substance: yes

| Provision | in the record | in the text |
|---|---|---|
| 1. *Scope* stops excluding structured output and names `SPEC-MOK-006` | yes | 3/3 phrases |
| 2. *Actors* adds the filesystem as a destination, never a source | yes | 2/2 phrases |
| 3. *Inputs* takes `--events-path` in the synopsis and one bullet | yes | 3/3 phrases |
| 4. *Help output* gains the option’s entry, in order | yes | 3/3 phrases |
| 5. *Outputs* adds the stream and leaves the text stream unaffected | yes | 3/3 phrases |
| 6. *Error and recovery behavior* adds the sink failures and the removal | yes | 3/3 phrases |
| 7. *Security and privacy properties* records the one path, and what no record carries | yes | 2/2 phrases |
| 8. *Performance and capacity* records the stream as write-only | yes | 1/1 phrases |
| 9. *Observability* adds a byte-identical record stream | yes | 1/1 phrases |
| 10. *Compatibility and migration* names the schema version | yes | 2/2 phrases |
| 11. *Explicitly unspecified decisions* records the stream as governed | yes | 2/2 phrases |

### `SPEC-MOK-002`

- rows dated 2026-08-20: **1**, of which **1** name this work order
- approval recorded in the row: yes
- records that the implementation agent wrote the text and decided no substance: yes

| Provision | in the record | in the text |
|---|---|---|
| 1. Rule 4 carries the literal signature with the one new parameter | yes | 2/2 phrases |
| 2. Rule 5's `execute` row reworded, the enumeration otherwise untouched | yes | 1/1 phrases |
| 3. Rule 5's mechanical check restated as two greps | yes | 3/3 phrases |
| 4. Rule 6 **not** amended, and the omission recorded at the rule | yes | 2/2 phrases |
| 5. *Scope* and *Compatibility and migration* name `SPEC-MOK-006` | yes | 3/3 phrases |

### The one provision that amends by deletion

No phrase search can show that a phrase is gone. The sentence that carried it is located
instead and its contents asserted, anchored on a phrase that must still be at the far end of
the list being inspected, so that a capture which stopped short fails rather than reporting an
absence it never looked for.

| Artifact | sentence | located, and the anchor in it | no longer names |
|---|---|---|---|
| `SPEC-MOK-001` | *Scope*'s excluded list no longer names structured output | yes, 257 characters through `user interface` | yes |

All 13 controls on the checks themselves held, so no line above is a check that looked for
nothing:

- ok — a provision stated in the row and carried by the body is reported clean
- ok — a provision the record does not state is reported
- ok — a provision absent from the body is reported
- ok — the record's own prose cannot satisfy a body check
- ok — a deletion check whose sentence is not found is reported, not passed
- ok — a deletion check truncated before its anchor is reported, not passed
- ok — a forbidden phrase still present in the sentence is reported
- ok — the row reader finds rows in order and ignores prose
- ok — a row outstanding in its own status cell is told from one that mentions the word
- ok — the ADR bullet counter ignores wrapped continuations and separates the record row
- ok — a row claiming a count in words is read, and one claiming none returns None
- ok — exactly one artifact has a declared count discrepancy, so the others cannot use it
- ok — every declared discrepancy carries at least two assertions and a reading

## 3. The provision counts each row claims, against what the ADR lists

A row that claims twelve provisions while the ADR lists thirteen bullets is a discrepancy in
one of the two, and it is worth measuring because the count is the only figure a reader can
check quickly. The ADR's last bullet for each document is the instruction to add an
amendment-record row, which is the record of the amendment rather than a provision of it, so
both figures are reported.

| Artifact | bullets in `ADR-MOK-005` | of those, provisions | the row claims | this file checks | agrees |
|---|---|---|---|---|---|
| `ARCH-MOK-001` | 13 | 12 | 12 | 12 | yes |
| `SPEC-MOK-001` | 12 | 11 | 11 | 11 | yes |
| `SPEC-MOK-002` | 5 | 4 | 5 | 5 | disclosed below |

`ARCH-MOK-001` and `SPEC-MOK-001` agree on all three counts. `SPEC-MOK-002` does not, and the
disagreement is declared rather than absorbed.

### Where the artifacts disagree with each other

Each disagreement below is pre-declared in the script with the assertions that establish it. An
undeclared one is a finding, and a declared one whose assertions do not hold is also a finding —
otherwise this section would be a way to silence a check rather than a way to record a fact.

**`ADR-MOK-005` attributes the signature to rule 5, and the signature is rule 4's.**

- `ADR-MOK-005` names rule 5 for the signature — confirmed
- `SPEC-MOK-002` rule 5 places the signature in rule 4 — confirmed
- the amendment row records rule 4 as amended — confirmed
- the amendment row records rule 5 as amended separately — confirmed

The substance is present and is more than the ADR named. Because the signature literal is
rule 4's and the enumeration row is rule 5's, one bullet had to become two amendments, and the
row states them as two — which is why it claims five where the ADR lists four. The ADR was
written by the implementation agent and this is an imprecision in its location-naming, not a
provision anybody approved and nobody made. Recorded in `completion-summary.md`.

**`ADR-MOK-005` counts the `SPEC-MOK-001` list as nine and lists eleven provisions.**

- the prose states the arithmetic — confirmed
- it names the four it added — confirmed

Five seams plus four additions is nine, and the list below that sentence carries eleven
provisions. The two the arithmetic does not account for are *Security and privacy properties*
and *Performance and capacity*, and both are in the ADR's list, in `SPEC-MOK-001`'s amendment
row and in its body — provisions 7 and 8 of section 2 above. So the defect is in the sentence
that counts them, not in the work. Recorded in `completion-summary.md`.

## 4. Amended beyond the approved list

`ADR-MOK-005` names three documents to amend and `WO-MOK-012` makes all three approval
preconditions. A fourth was amended, and it is neither silent nor approved.

**`SPEC-MOK-004`** — rule 11 records the workspace test census and delegates its own correction.

- rows dated 2026-08-20 naming this work order: **1**
- the row states all 10 of its required phrases: yes
- the body carries all 7 of its required figures: yes
- the row records the amendment as **not approved** rather than claiming approval: yes

**Why this is disclosed rather than resolved.** Rule 11 states the obligation in its own text
— "a work order that adds a test corrects these figures here, and one that loses a test has a
defect" — so the rule delegates the correction to the work order that causes it, and every
figure in the row is a measured outcome rather than a decision: `cargo test`'s per-target
counts in `gates.txt`, the name-by-name reconciliation in
`analysis/census-reconciliation.txt`, and the enumeration in `interface.txt`. What the rule
cannot delegate is the judgement that `SPEC-MOK-004` belongs in this chain at all. The owner
approved three amendments on 2026-08-20 and `SPEC-MOK-004` was not among them, so its row
records the amendment as **OUTSTANDING** for the owner's confirmation rather than claiming an
approval nobody gave. `WO-MOK-011` faced the same situation on the same rule and recorded it
the same way, which is the precedent this follows rather than a new reading.

## 5. The earlier layer, left where it was

Amendments were already **OUTSTANDING** in three of these four documents before this work
began. This work order neither resolves nor disturbs any of them, which is a checkable claim:
every amendment row that existed at the base commit, compared against the file now, in order,
and the rows whose own status cell declares them outstanding counted on both sides.

| Artifact | rows at the base | rows now | the base rows unchanged and in order | rows reading OUTSTANDING then, now |
|---|---|---|---|---|
| `ARCH-MOK-001` | 5 | 6 | yes | 1, then 1 |
| `SPEC-MOK-001` | 10 | 11 | yes | 0, then 0 |
| `SPEC-MOK-002` | 4 | 5 | yes | 1, then 1 |
| `SPEC-MOK-004` | 5 | 6 | yes | 1, then 1 |

The three rows still outstanding, each quoted from its own subject cell so that this file names
what it carries forward rather than counting it:

- `ARCH-MOK-001`, 2026-08-18 — Narrowed the prohibition on public items from "mutable **or owned** authoritative state" to a mutable borrow of, or a reference into, that state, a... It belongs to `WO-MOK-005`.
- `SPEC-MOK-002`, 2026-08-18 — Four provisions amended so that the terminal observer of `SPEC-MOK-003` can be conformed to. It belongs to `WO-MOK-005`.
- `SPEC-MOK-004`, 2026-08-19 — Recorded figures and two subject lines corrected, because `SPEC-MOK-003` rule 5 as amended the same day replaced the observer's four-row layout-tie... Its status cell names no work order.

Each awaits the technical owner, which the repository owner also is, and none of them is this
work order's to pay. They are named here because carrying an outstanding amendment forward is a
fact about the governance state this change is verified against, and a count alone would let
the reader believe they had been looked at.

The rows this work order adds are appended. No earlier row is edited, reordered, renumbered,
summarised or folded into a later one, and no row that read **OUTSTANDING** reads anything
else now: the last column counts the base rows only, on both sides, so an appended row cannot
inflate it. Each appended row states which outstanding rows above it are left untouched — a
reference to a status, not a status — and `is_outstanding` reads the row's own status cell
rather than searching the row for the word, which is the control that tells the two apart.

## 6. What must not have changed at all

| Artifact | changed since the base commit | line endings only | why it must not be |
|---|---|---|---|
| `SPEC-MOK-003` | no | yes | the observer behavior contract; this work order adds a stream the observer does not read |
| `SPEC-MOK-005` | no | yes | not in this chain; not opened |
| `ARCH-MOK-002` | no | yes | the observer architecture; outside `ARCH-MOK-001`'s boundary and outside this change |
| `ADR-MOK-001` | no | yes | the trust boundary `ADR-MOK-005` supersedes nothing of |
| `ADR-MOK-002` | no | yes | the enumerated-interface decision; not re-opened |
| `ADR-MOK-003` | no | yes | the two-package split; not re-opened |
| `ADR-MOK-004` | no | yes | not in this chain; not opened |
| `VREC-MOK-001` | no | yes | commit-bound record; not re-opened |
| `VREC-MOK-002` | no | yes | commit-bound record; not re-opened |
| `VREC-MOK-003` | no | yes | commit-bound record; not re-opened |
| `VREC-MOK-004` | no | yes | commit-bound record; not re-opened |
| `VREC-MOK-005` | no | yes | commit-bound record; not re-opened |
| `VREC-MOK-006` | no | yes | commit-bound record; not re-opened |
| `VREC-MOK-007` | no | yes | commit-bound record; not re-opened |
| `VREC-MOK-008` | no | yes | commit-bound record; not re-opened |
| `VREC-MOK-009` | no | yes | commit-bound record; not re-opened |
| `VREC-MOK-010` | no | yes | commit-bound record; not re-opened |
| `VREC-MOK-011` | no | yes | commit-bound record; not re-opened |

The middle column is why this table reads content rather than raw bytes: this clone has
`core.autocrlf = true`, so a tracked text file is CRLF in the working tree and LF in the blob,
and every one of these files differs from its blob in exactly that way and no other. The check
is run both ways — content equal after folding CRLF to LF, and raw bytes equal once both sides
are folded — so the normalization cannot absorb an edit.

No verification record is re-opened. `VREC-MOK-001` through `VREC-MOK-011` stand as they are,
and the three amendments listed as outstanding in section 5 are carried forward again,
unchanged and unpaid.

## Result

**RESULT: PASS** — `ADR-MOK-005` is `approved`, every artifact in the chain carries the
status it should, all twenty-eight provisions are present in both the amendment record and
the document text, the provision that amends by deletion is shown to have deleted, the
amendment beyond the approved list is named with the rule that obliges it and recorded as
unapproved rather than claimed, the earlier layer's rows are unchanged and in order with
their three OUTSTANDING markers intact and named, and the eighteen documents that must not
have moved are identical in content — and in raw bytes but for the CRLF this clone checks
out.

Twenty-eight, not twenty-seven: `ADR-MOK-005` lists twenty-seven provisions across the three
documents, and the twenty-eighth is the rule 4 amendment its rule 5 bullet requires without
naming rule 4. That is one of the two places where the artifacts disagree with each other,
and both are recorded in section 3 rather than reconciled. Neither is a provision the owner
approved and the work did not make; both are imprecisions in the text of the ADR, which the
implementation agent wrote.

What this file does **not** establish: that this change is verified. Oracle 7 is one of seven,
and `VER-MOK-012`'s eight manual assessments are recorded **OUTSTANDING** in
`manual-assessment.md`. An amendment being approved is not a judgement that the amended
contract is satisfied; the technical owner's confirmation of the amended component
boundaries, prohibited patterns, dependency prohibition and conformance checks is manual
assessment territory and is not recorded here.
