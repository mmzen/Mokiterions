# WO-MOK-011 amendment approvals — oracle 5, governance half

`VER-MOK-011`'s fifth oracle is the governance state of the artifacts this change amends:
an amendment nobody approved is not a specification, and its absence fails the contract
however the code behaves. This file is generated by `analysis/amendments.py`, which reads the
artifacts and the git history; what it checks, and why each check is not a formality, is in
that script's header. The oracle's other half — the engine's public interface enumerated item
for item at both commits, which is what checks `WO-MOK-011`'s claim that `SPEC-MOK-002` needs
no amendment — is `interface.txt`.

Everything below is measured against `524a6758d74b5240079959e9827ea40a7af22a30`, the commit this work started from, and every
row this work order adds is dated `2026-08-19`.

## 1. The chain's governance state

| Artifact | status | expected | updated | ok |
|---|---|---|---|---|
| `INT-MOK-008` | `approved` | `approved` | 2026-08-19 | yes |
| `CAP-MOK-008` | `approved` | `approved` | 2026-08-19 | yes |
| `REQ-MOK-040` | `approved` | `approved` | 2026-08-19 | yes |
| `REQ-MOK-041` | `approved` | `approved` | 2026-08-19 | yes |
| `VER-MOK-011` | `approved` | `approved` | 2026-08-19 | yes |
| `WO-MOK-011` | `in_progress` | `in_progress` | 2026-08-19 | yes |
| `SPEC-MOK-001` | `approved` | `approved`, amended | 2026-08-19 | yes |
| `SPEC-MOK-002` | `approved` | `approved` | 2026-08-19 | yes |
| `SPEC-MOK-003` | `approved` | `approved`, amended | 2026-08-19 | yes |
| `SPEC-MOK-004` | `approved` | `approved`, amended | 2026-08-19 | yes |

`SPEC-MOK-002` is in the table because this work order names it: it is `approved` and
**unamended**, which is a claim checked in section 5 and in `interface.txt` rather than
asserted here. The work order is `in_progress` and not `complete`: a work order is closed by a
verification record that binds a commit, and that record is written after the commit it names.

## 2. The amendment record against the approved list

Each provision `WO-MOK-011` states in full is looked for twice — in the amendment record’s
2026-08-19 row, and in the specification’s body with every amendment row removed — because a
record that claimed an amendment the text does not carry would otherwise satisfy both checks
with one sentence.

### `SPEC-MOK-001`

- rows dated 2026-08-19: **4**, of which **1** name this work order
- approval recorded in the row: yes
- records that the implementation agent wrote the text and decided no substance: yes

| Provision | in the record | in the text |
|---|---|---|
| 1. *State model / Mokiterion* gains the name | yes | 2/2 phrases |
| 2. A *Name* subsection fixes the twelve, the domain and the three properties | yes | 5/5 phrases |
| 3. *Time and entropy* records that naming performs no draw at all | yes | 1/1 phrases |
| 4. *Data and interface contracts*: `name` first, `waste_tolerance` last, reported once | yes | 4/4 phrases |
| 5. Rule 1 places the assignment at agent creation | yes | 1/1 phrases |

### `SPEC-MOK-003`

- rows dated 2026-08-19: **4**, of which **1** name this work order
- approval recorded in the row: yes
- records that the implementation agent wrote the text and decided no substance: yes

| Provision | in the record | in the text |
|---|---|---|
| 1. Rule 2's glyph tables become the name's first character, anticipation retained | yes | 4/4 phrases |
| 2. Rule 4 carries the name first, and line two is measured to be untouched | yes | 4/4 phrases |
| 3. Rule 10 presents the name; item 7 loses it and keeps the six | yes | 2/2 phrases |

Two provisions amend by deletion, and no phrase search can show that a phrase is gone. The
sentence that carried it is located instead and its contents asserted, each anchored on a
phrase that must still be at the far end of the list being inspected, so that a capture which
stopped short fails rather than reporting an absence it never looked for.

| Artifact | sentence | located, and the anchor in it | no longer names |
|---|---|---|---|
| `SPEC-MOK-003` | rule 10 item 7's list of values the engine does not compute no longer names the name | yes, 187 characters through `per-agent entropy` | yes |
| `SPEC-MOK-003` | rule 2's active Mokiterion glyph row no longer assigns a digit | yes, 114 characters through `first character` | yes |

All 9 controls on the checks themselves held, so no line above is a check that looked for
nothing:

- ok — a provision stated in the row and carried by the body is reported clean
- ok — a provision the record does not state is reported
- ok — a provision absent from the body is reported
- ok — the record's own prose cannot satisfy a body check
- ok — a deletion check whose sentence is not found is reported, not passed
- ok — a deletion check truncated before its anchor is reported, not passed
- ok — a forbidden word still present in the sentence is reported
- ok — the row reader finds rows in order and ignores prose
- ok — a row outstanding in its own status cell is told from one that mentions the word

## 3. What was amended beyond the approved list

`WO-MOK-011` states amendments to two specifications. A third was amended: `SPEC-MOK-004`,
whose rules 9, 10 and 11 record the test census. It is not in the work order’s list, and it is
not a silent amendment either — the rule it amends is the rule that requires it.

**`SPEC-MOK-004`** — recorded test-count figures corrected for the twelve tests this work
order adds; rule 6’s interface figures re-checked and unchanged.

- rows dated 2026-08-19 naming this work order: **1**
- approval recorded in the row: yes
- states that every figure is a measured outcome rather than a decision: yes
- rule 6 recorded unchanged at 94 items, 118 `pub` lines, 24 public fields: yes

**Why this is a discharge and not an unapproved amendment.** Rule 11 states the obligation in
its own text — "a work order that adds a test corrects these figures here, and one that loses a
test has a defect" — so the rule delegates the correction to the work order that causes it. The
row records measurements: `cargo test`’s per-target counts and the enumeration in
`interface.txt`. The two decisions those figures reflect are the technical owner’s of
2026-08-19 and are recorded in `WO-MOK-011`: that the name reaches the observer through the
retained event stream rather than a new public interface item, which is why `Observer::name_of`
is `pub(crate)` and rule 6 is unchanged; and that a test is placed in the tier its required
access puts it in, which `SPEC-MOK-004` rules 8 to 12 already fix and which forbids widening an
item to relocate a test. **No provision of `SPEC-MOK-004` other than the recorded figures
changed, and rule 6 — the interface contract — was re-measured rather than amended.**

## 4. The earlier layer, left where it was

Amendments were already **OUTSTANDING** in all four specifications before this work began, most
of them `WO-MOK-005`'s and `WO-MOK-007`'s. This work order neither resolves nor disturbs them,
which is a checkable claim: every amendment row that existed at the base commit, compared
against the file now, in order, and the rows carrying the word counted on both sides.

| Artifact | rows at the base | rows now | the base rows unchanged and in order | rows reading OUTSTANDING then, now |
|---|---|---|---|---|
| `SPEC-MOK-001` | 9 | 10 | yes | 1, then 1 |
| `SPEC-MOK-002` | 4 | 4 | yes | 3, then 3 |
| `SPEC-MOK-003` | 8 | 9 | yes | 4, then 4 |
| `SPEC-MOK-004` | 3 | 4 | yes | 2, then 2 |

The rows this work order adds are appended. No earlier row is edited, reordered, renumbered,
summarised or folded into a later one, and no row that read **OUTSTANDING** reads anything else
now: the last column counts the base rows only, on both sides, so an appended row cannot inflate
it. Of the 3 appended rows, 2 contain the word, because each states which outstanding rows above
it are left untouched — a reference to a status, not a status. None of the appended rows carries
**OUTSTANDING** as its own state; each records an approval of this date.

## 5. What must not have changed at all

| Artifact | changed since the base commit | line endings only | why it must not be |
|---|---|---|---|
| `SPEC-MOK-002` | no | yes | `WO-MOK-011` claims it needs no amendment; oracle 5's interface half is what checks the claim |
| `VREC-MOK-001` | no | yes | commit-bound record; not re-opened |
| `VREC-MOK-002` | no | yes | commit-bound record; not re-opened |
| `VREC-MOK-003` | no | yes | commit-bound record; not re-opened |
| `VREC-MOK-004` | no | yes | commit-bound record; not re-opened |
| `VREC-MOK-005` | no | yes | the record whose gate the owner overrode under `WO-MOK-007`; still `ready`, its own amendments still outstanding |
| `VREC-MOK-006` | no | yes | measured 97 interface items and 169 tests, both correct at its commit |
| `VREC-MOK-007` | no | yes | commit-bound record; not re-opened |
| `ARCH-MOK-001` | no | yes | no architecture amendment is required by this work order |
| `ARCH-MOK-002` | no | yes | the same |

The middle column is why this table reads content rather than raw bytes: this clone has
`core.autocrlf = true`, so a tracked text file is CRLF in the working tree and LF in the blob,
and every one of these files differs from its blob in exactly that way and no other. The check
is run both ways — content equal after folding CRLF to LF, and raw bytes equal once both sides
are folded — so the normalization cannot absorb an edit.

`VREC-MOK-005` is still `ready` and `VREC-MOK-006` and `VREC-MOK-007` are untouched. This work
order does not approve their amendments, does not perform their manual assessments and does not
transition their work orders. The debt carried forward under `WO-MOK-007` is carried forward
again, unchanged and unpaid.

## Result

**RESULT: PASS** — every artifact in the chain carries the status it should, all eight provisions
`WO-MOK-011` states are present in both the amendment record and the specification text, the
two provisions that amend by deletion are shown to have deleted, the amendment beyond the
approved list is named with the rule that obliges it, the earlier layer's rows are unchanged
and in order with their OUTSTANDING markers intact, and `SPEC-MOK-002`, both architecture
documents and all seven verification records are unchanged — identical in content, and in raw
bytes but for the CRLF this clone checks out.

What this file does **not** establish: that oracle 5 is satisfied. Its other half is the
interface enumeration in `interface.txt`, and `VER-MOK-011`'s manual assessment 5 is recorded
**OUTSTANDING** in `manual-assessment.md`.
