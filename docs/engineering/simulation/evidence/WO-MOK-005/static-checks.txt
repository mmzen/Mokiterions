# Static and architecture checks — WO-MOK-005

Re-captured on 2026-08-19 against the working tree that carries the amended rule 5, on
`feature/wo-mok-005-layout-axes` with `origin/master` at 05dc6ac merged in. The previous
capture is superseded twice over: `WO-MOK-006` moved the engine package into
`mokiterions-core/`, so the `cargo tree` path below changed without anything in this work
order changing, and this work order's own edits touch `layout.rs`, `render.rs`,
`verification.rs` and three test files.

Nothing is patched for these runs, and no scratch measurement harness is present in the tree
they were taken from: the oracle that produced `layout-and-viewports.txt` and `frames.txt` was
deleted before the first command below ran, so `mokiterions-tui/tests/` holds the eight files
`SPEC-MOK-004` rule 9 names and nothing else. Both packages are cleaned before each
invocation, so no line is a cache hit standing in for a check that did not run.

## Toolchain

$ cargo --version
cargo 1.97.1 (c980f4866 2026-06-30)
$ rustc --version
rustc 1.97.1 (8bab26f4f 2026-07-14)

## The workspace contains exactly two packages

$ cargo tree --workspace --depth 0
Mokiterions v0.1.0 (C:\Users\mathi\RustroverProjects\Mokiterions\mokiterions-core)

mokiterions-tui v0.1.0 (C:\Users\mathi\RustroverProjects\Mokiterions\mokiterions-tui)

## cargo fmt --all -- --check

(no differences)
exit=0

## cargo clippy -p Mokiterions --all-targets --all-features -- -D warnings

The engine package alone and first, so a finding in it cannot be masked by the observer's
build or served from its cache.

    Checking Mokiterions v0.1.0 (C:\Users\mathi\RustroverProjects\Mokiterions\mokiterions-core)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.72s
exit=0

## cargo clippy --workspace --all-targets --all-features -- -D warnings

    Checking Mokiterions v0.1.0 (C:\Users\mathi\RustroverProjects\Mokiterions\mokiterions-core)
    Checking mokiterions-tui v0.1.0 (C:\Users\mathi\RustroverProjects\Mokiterions\mokiterions-tui)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 1.32s
exit=0

## harnessctl validate

$ harnessctl validate
Engineering artifact validation: PASS
Artifacts: 68 | Errors: 0 | Warnings: 0
Planes: structure E0/W0 | governance E0/W0 | policy E0/W0 | maintenance E0/W0
exit=0

`scripts/validate_engineering_artifacts.py` prints the same four lines and also exits 0. The two
are the same validator reached two ways, so agreement here is a consistency check rather than a
second opinion.

## harnessctl doctor

81 lines, every one of them PASS, no line of any other severity. Every managed file reports
`unchanged`, which is the check that matters for the two artifacts this work order amends: the
amendments are to repository-owned specifications, and no file the harness manages was edited to
accommodate them.
exit=0

## harnessctl preflight --work-order WO-MOK-005 --phase review

Harness preflight: PASS
Phase: review
Work order: WO-MOK-005 (in_progress)
Assurance classification: commit-bound verification required, decided by the engineering owner.
exit=0

The work order is still `in_progress` and the preflight is the review-phase form, which is the
state this evidence is captured for. Preflight is derived, read-only evidence; it approves
nothing.

## scripts/generate_harness_dashboard.py --root .

Harness Explorer generation: PASS | Artifacts: 68 | Relations: 211 | Errors: 0 | Warnings: 8

Warnings are not errors here, and the honest way to read 8 is against a baseline. The same
generator, run against a clean extraction of `origin/master` at 05dc6ac into a scratch directory:

Harness Explorer generation: PASS | Artifacts: 68 | Relations: 211 | Errors: 0 | Warnings: 6

Six pre-exist. Five are `WO-MOK-001` through `WO-MOK-004` and `WO-MOK-006` reporting no evidence
document keyed to their own ID, and the sixth is `ADR-MOK-001` predating `ARCH-MOK-001`. The two
this work adds are one finding stated twice:

    ARCH-MOK-002 predates newer declared conforms_to target SPEC-MOK-003 and may require reassessment.
    ARCH-MOK-002 predates newer declared conforms_to target SPEC-MOK-004 and may require reassessment.

Both follow mechanically from the amendments moving `SPEC-MOK-003` and `SPEC-MOK-004` to
2026-08-19 while `ARCH-MOK-002` stays at 2026-08-18. `ARCH-MOK-002` is deliberately not touched:
the last paragraph of disclosure 16 in `completion-summary.md` states why, and why the expected
outcome of the reassessment is a re-affirmation rather than a change.
