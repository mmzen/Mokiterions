# VER-MOK-012: the test census, split by the binary that ran each test
#
# baseline log:  C:/Users/mathi/m-test-run-pr31.txt
# candidate log: C:/Users/mathi/m-test-run.txt
# command: python docs/engineering/simulation/evidence/WO-MOK-019/analysis/census-by-target.py \
#              <baseline-test-log> <candidate-test-log> <output-file>
#
# The primary census, from WO-MOK-011/analysis/census.py reused unmodified, qualifies every
# unit-test target as `unittests` and so collapses four targets into one. This pass qualifies
# by the test binary instead. Both are reported so that the collapse is visible rather than
# assumed harmless.

  target                                                      before   after   delta
  mokiterions  unittests src/lib.rs                               68      96     +28
  Mokiterions  unittests src/main.rs                               0       0      +0
  cli  tests/cli.rs                                               16      18      +2
  decisions  tests/decisions.rs                                    1       3      +2
  density  tests/density.rs                                        2       2      +0
  naming  tests/naming.rs                                          3       3      +0
  process  tests/process.rs                                        6       7      +1
  records  tests/records.rs                                       17      17      +0
  termination  tests/termination.rs                                4       5      +1
  viability  tests/viability.rs                                    2       5      +3
  mokiterions_tui  unittests src/lib.rs                           31      33      +2
  mokiterions_tui  unittests src/main.rs                           8       8      +0
  authority  tests/authority.rs                                    4       4      +0
  export  tests/export.rs                                          7       7      +0
  layout  tests/layout.rs                                         10      11      +1
  options  tests/options.rs                                        8       8      +0
  render  tests/render.rs                                         12      22     +10
  spatial  tests/spatial.rs                                        7       7      +0
  state  tests/state.rs                                           21      21      +0
  verification  tests/verification.rs                             19      21      +2
  doc-tests mokiterions                                            0       0      +0
  doc-tests mokiterions_tui                                        0       0      +0
  TOTAL                                                          246     298     +52

# 54 addition(s), 2 removal(s) under this qualifier

  + cli                the_social_policy_is_selectable_and_does_not_become_the_default
  + cli                the_unknown_policy_diagnostic_names_every_accepted_value
  + decisions          every_targeted_verb_applies_somewhere_in_the_declared_matrix
  + decisions          the_acting_order_is_one_ascending_pass_per_tick_under_the_social_source
  + layout             the_log_is_six_rows_wherever_it_is_present
  + layout             the_reference_roster_interior_holds_the_whole_population
  + mokiterions        simulation::tests::a_defender_below_its_attackers_identifier_answers_on_the_next_tick
  + mokiterions        simulation::tests::a_full_health_mokiterion_falls_in_four_strikes_at_most_and_ten_at_least
  + mokiterions        simulation::tests::a_mokiterion_killed_mid_tick_takes_no_further_opportunity
  + mokiterions        simulation::tests::a_rejected_targeted_proposal_mutates_nothing_and_reports_nothing
  + mokiterions        simulation::tests::a_resolution_touches_nobody_but_its_two_parties
  + mokiterions        simulation::tests::a_strike_that_empties_health_kills_through_rule_13_and_no_other_path
  + mokiterions        simulation::tests::a_surrender_buys_no_immunity_and_is_paid_again
  + mokiterions        simulation::tests::a_surrender_forfeits_half_its_own_satiety_and_discards_what_does_not_fit
  + mokiterions        simulation::tests::a_targeted_move_is_a_rule_8_move_on_rule_5s_axis
  + mokiterions        simulation::tests::a_threat_composes_with_rule_12_in_turn_order_and_outlasts_its_tick
  + mokiterions        simulation::tests::a_threat_moves_the_targets_fear_and_nothing_else
  + mokiterions        simulation::tests::answering_one_attack_closes_the_window_on_every_attack_in_it
  + mokiterions        simulation::tests::both_parties_may_die_within_one_tick
  + mokiterions        simulation::tests::branch_one_answers_the_earliest_attacker_and_precedes_every_other_branch
  + mokiterions        simulation::tests::branch_one_answers_the_first_attack_at_the_specified_fear_thresholds
  + mokiterions        simulation::tests::branch_two_puts_survival_before_society_and_draws_nothing
  + mokiterions        simulation::tests::branches_four_and_five_choose_by_distance_then_by_the_engagement_threshold
  + mokiterions        simulation::tests::contact_and_resolution_span_the_territory_boundary
  + mokiterions        simulation::tests::contact_is_recomputed_from_position_and_needs_two_living_neighbors
  + mokiterions        simulation::tests::every_targeted_rejection_names_the_first_unmet_condition
  + mokiterions        simulation::tests::exchanging_the_two_identifiers_changes_no_outcome
  + mokiterions        simulation::tests::no_resolution_and_no_targeted_move_touches_the_shared_stream
  + mokiterions        simulation::tests::no_source_but_rule_26s_opens_the_suffered_window
  + mokiterions        simulation::tests::strike_damage_is_the_strikers_own_condition_and_the_cost_is_flat
  + mokiterions        simulation::tests::the_action_vocabulary_is_eleven_kinds_and_seven_name_a_target
  + mokiterions        simulation::tests::the_social_source_draws_at_most_once_and_never_for_a_targeted_proposal
  + mokiterions        simulation::tests::the_suffered_window_opens_in_resolution_order_and_closes_at_the_next_opportunity
  + mokiterions        simulation::tests::with_no_company_and_an_empty_record_the_social_source_is_the_individual_source
  + mokiterions_tui    render::tests::a_ten_point_step_moves_the_fill_at_the_reference_viewport
  + mokiterions_tui    render::tests::every_bar_width_the_roster_can_produce_resolves_a_ten_point_step
  + process            the_social_source_runs_to_completion_and_exits_successfully
  + render             a_declining_mokiterion_shows_a_declining_bar
  + render             every_living_mokiterion_has_an_entry_at_the_reference_viewport
  + render             no_entry_is_lost_silently_at_any_viewport_presenting_the_roster
  + render             the_announcement_and_the_hint_read_nothing_but_the_viewport
  + render             the_announcement_appears_and_disappears_with_the_pane_it_names
  + render             the_announcement_is_emphasised_and_the_optional_segments_are_not
  + render             the_announcement_states_the_axis_and_the_value_the_layout_decides_presence_from
  + render             the_hint_displaces_neither_the_announcement_nor_the_footer
  + render             the_hint_is_present_after_two_hundred_ticks_in_both_run_states
  + render             the_key_binding_hint_is_on_screen_in_the_first_frame_at_every_viewport
  + termination        a_long_run_is_bounded_under_the_social_source
  + verification       no_source_confined_to_the_valid_action_list_has_a_proposal_rejected
  + verification       the_announcement_and_the_hint_survive_the_loss_of_colour
  + verification       the_social_source_is_rejected_only_as_the_specification_admits
  + viability          no_identifier_series_is_monotone_in_identifier
  + viability          survival_by_turn_position_stays_inside_the_stated_bound
  + viability          the_social_source_keeps_the_world_habitable_and_combat_lethal
  - layout             the_log_is_ten_rows_only_where_both_thresholds_are_met
  - verification       no_shipped_decision_source_has_a_proposal_rejected

# A rename appears here as one addition and one removal, and this comparison has
# 2 removal(s), named above. Each is a baseline test that is renamed,
# moved between targets, or deleted; which of the three it is does not follow from two
# logs and is not asserted here. Where a rename is the explanation, its new name is one
# of the additions above and the pair is a rename rather than a loss.

# ---- full text of this script, retained as VER-MOK-012 requires ----

"""VER-MOK-012: the test census split by the binary that ran it, and reconciled per target.

Usage, from the repository root:

    python docs/engineering/simulation/evidence/WO-MOK-019/analysis/census-by-target.py \
        <baseline-test-log> <candidate-test-log> <output-file>

## Why a second pass over the same two logs

The census this packet retains is produced by `WO-MOK-011/analysis/census.py`, reused unmodified, so
that the two packets' censuses are comparable line for line. That tool qualifies each test name by the
first word after `Running`, which for a unit-test target is the literal word `unittests`. The
workspace has four unit-test targets -- the engine's library and binary and the observer's library and
binary -- so all four collapse into one qualifier, which is exactly the collision the tool's own
docstring warns about. It is a pre-existing limitation and `WO-MOK-011`'s retained census has it too.

This pass qualifies by the test binary Cargo names on the same line, which distinguishes all four, and
reports:

  * every target's test count before and after, with the delta;
  * the additions and removals under that finer qualifier.

If the finer pass agrees with the primary one on the additions and removals, the collapse changed
nothing here, and that agreement is the point of running both. If it disagrees, a test moved between
targets under one name and the primary census could not have seen it.

The build hash in each binary's file name is dropped: it is a function of the compilation, not of the
test, and comparing two trees would otherwise report every target as removed and re-added.
"""

import io
import re
import sys

RUNNING = re.compile(r'^\s+(Running|Doc-tests)\s+(.*)$')
RESULT = re.compile(r'^test result: \w+\. (\d+) passed')
TEST = re.compile(r'^test (\S+) \.\.\. \w+')
BINARY = re.compile(r'deps/([A-Za-z_]+)-[0-9a-f]+\.exe')


def read(path):
    """(per-target reported counts, per-target test names) for one `cargo test` log."""
    label, counts, names = None, [], []
    for line in io.open(path, encoding='utf-8', errors='replace'):
        line = line.rstrip('\n')
        running = RUNNING.match(line)
        if running:
            kind, text = running.group(1), running.group(2).replace('\\', '/')
            binary = BINARY.search(text)
            source = text.split(' (')[0]
            if kind == 'Doc-tests':
                label = f'doc-tests {source}'
            else:
                label = f'{binary.group(1)}  {source}' if binary else source
            continue
        test = TEST.match(line)
        if test:
            names.append((label, test.group(1)))
            continue
        result = RESULT.match(line)
        if result:
            counts.append((label, int(result.group(1))))
    return counts, names


def main():
    baseline_log, candidate_log, output_file = sys.argv[1:4]
    before, before_names = read(baseline_log)
    after, after_names = read(candidate_log)

    ordered, seen = [], set()
    for label, _ in before + after:
        if label not in seen:
            seen.add(label)
            ordered.append(label)
    b, a = dict(before), dict(after)

    added = sorted(set(after_names) - set(before_names))
    removed = sorted(set(before_names) - set(after_names))

    lines = [
        '# VER-MOK-012: the test census, split by the binary that ran each test',
        '#',
        f'# baseline log:  {baseline_log}',
        f'# candidate log: {candidate_log}',
        '# command: python docs/engineering/simulation/evidence/WO-MOK-019/analysis/'
        'census-by-target.py \\',
        '#              <baseline-test-log> <candidate-test-log> <output-file>',
        '#',
        '# The primary census, from WO-MOK-011/analysis/census.py reused unmodified, qualifies every',
        '# unit-test target as `unittests` and so collapses four targets into one. This pass qualifies',
        '# by the test binary instead. Both are reported so that the collapse is visible rather than',
        '# assumed harmless.',
        '',
        f'  {"target":<58} {"before":>7} {"after":>7} {"delta":>7}',
    ]
    for label in ordered:
        lines.append(
            f'  {label:<58} {b.get(label, 0):>7} {a.get(label, 0):>7} '
            f'{a.get(label, 0) - b.get(label, 0):>+7}'
        )
    lines.append(
        f'  {"TOTAL":<58} {sum(b.values()):>7} {sum(a.values()):>7} '
        f'{sum(a.values()) - sum(b.values()):>+7}'
    )
    lines.append('')
    lines.append(f'# {len(added)} addition(s), {len(removed)} removal(s) under this qualifier')
    lines.append('')
    for label, name in added:
        lines.append(f'  + {label.split("  ")[0]:<18} {name}')
    for label, name in removed:
        lines.append(f'  - {label.split("  ")[0]:<18} {name}')
    lines.append('')
    # The no-removal sentence was written unconditionally when this script was first used, because
    # the only comparison it had then been asked for had no removal. Comparing a merge against the
    # parent whose renames it inherits does have removals, and the sentence was then a false claim
    # printed by a verification instrument -- which is worse than a wrong figure in a document,
    # because a reader has no reason to doubt it. It is a condition now, and the no-removal branch is
    # word for word what it was, so every output taken before this correction reproduces byte for
    # byte from the logs it was taken from.
    if removed:
        lines.append(
            f'# A rename appears here as one addition and one removal, and this comparison has'
        )
        lines.append(
            f'# {len(removed)} removal(s), named above. Each is a baseline test that is renamed,'
        )
        lines.append(
            '# moved between targets, or deleted; which of the three it is does not follow from two'
        )
        lines.append(
            '# logs and is not asserted here. Where a rename is the explanation, its new name is one'
        )
        lines.append('# of the additions above and the pair is a rename rather than a loss.')
    else:
        lines.append(
            '# A rename would appear here as one addition and one removal. There is no removal, so no'
        )
        lines.append(
            '# baseline test was renamed, moved between targets, or deleted: every one of them is still'
        )
        lines.append('# present under its own name in its own binary.')
    lines.append('')
    lines.append('# ---- full text of this script, retained as VER-MOK-012 requires ----')
    lines.append('')
    lines.extend(io.open(__file__, encoding='utf-8').read().split('\n'))

    with io.open(output_file, 'w', encoding='utf-8', newline='\n') as handle:
        handle.write('\n'.join(lines) + '\n')

    print(
        f'{sum(b.values())} before, {sum(a.values())} after, {len(added)} added, '
        f'{len(removed)} removed, over {len(ordered)} targets; written to {output_file}'
    )
    return 1 if removed else 0


if __name__ == '__main__':
    sys.exit(main())

